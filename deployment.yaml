---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: azure-hackathon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: azure-hackathon
  template:
    metadata:
      labels:
        app: azure-hackathon
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: azure-hackathon
        image: projectnodejs.azurecr.io/projectnodejs:master-26
        resources:
          requests:
            cpu: 500m
            memory: 512Mi
          limits:
            cpu: 1024m
            memory: 512Mi
        ports:
        - containerPort: 3000
       
---
apiVersion: v1
kind: Service
metadata:
  name: public-svc
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: hackathon-public
